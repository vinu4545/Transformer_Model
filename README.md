# Transformer_Model
A hybrid sentiment classification model combining LLaMA 3B frozen embeddings with a Bidirectional LSTM layer, trained on the Sentiment140 dataset (1.6M tweets). Achieves superior accuracy over LSTM-only and LLaMA-only baselines. Built with PyTorch and HuggingFace Transformers, trained on Google Colab.
